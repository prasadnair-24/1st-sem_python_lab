# Exercise 3: Functions
# Task: Create a Python function that:

# Takes a list of numbers as input and returns the maximum number in the list.
# Test the function with a sample list.

def find_max(numbers):
    return max(numbers)

# Test the function
sample_list = [10, 20, 35, 50, 5]
print("Maximum number in the list:", find_max(sample_list))
